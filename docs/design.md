# Clipnote 設計書

> 最終更新：2026-08-04（v8）
> 対象：要件定義書 v5 の詳細設計（用語整理／ドメイン構成／UI方針／URL設計／セキュリティ設計／バリデーション／画面設計／データモデル／モノレポ構成）
> v8での変更点：コンテンツ配信（apps/content）の実装方針をHonoベースの軽量Workerとして明記。トークン検証とコンテンツ取得の処理フローを訂正・明確化（トークン検証自体はDBアクセス不要だが、本文取得にはD1読み取りが必要）

---

## 0. 用語整理

「コンテンツ／ページ／クリップ」の呼称が曖昧なため、ユーザー向け表示名と内部名を分離して整理する。

| 概念 | DB／コード内部名（変更なし） | ユーザー向け表示名 |
| --- | --- | --- |
| 個別のHTML/Markdown文書 | `pages` | **クリップ**（Clip） |
| 複数クリップの束 | `collections` | **コレクション** |
| 中間テーブル | `collection_pages` | （UI上には露出しない） |

- DB・URL（`pages` / `/p/`）は変更しない。UUIDベースで意味を持たないパスのため、表示名変更の影響を受けない。
- UI文言のみ「ページ」→「クリップ」に統一する。

---

## 1. ドメイン構成

Web本体ドメインが確定しているため、コンテンツ配信・MCPともに`paritto.dev`のサブドメインで統一する。

| 用途 | ドメイン |
| --- | --- |
| Web本体（管理画面／公開ページ外枠／LP） | `clipnote.paritto.dev` |
| コンテンツ配信（iframe内） | `content.clipnote.paritto.dev` |
| MCPサーバー（次フェーズ） | `mcp.clipnote.paritto.dev` |

### 実装時の注意点（Cookieスコープ）

親ドメイン`paritto.dev`を共有するため、4章のiframe隔離（別ドメイン配信）による多層防御の効果を実質的に確保するには、**better-authのセッションCookieの`Domain`属性を`clipnote.paritto.dev`というホスト単体にスコープする**（`Domain`属性を省略し、親ドメイン全体に広げない）ことを実装時に必ず確認する。

---

## 2. UI／UXデザイン方針

画面の性質によってデザインシステムを使い分ける。

| 画面 | 採用方針 | 理由 |
| --- | --- | --- |
| `/admin`配下（管理画面） | **shadcn/ui（挙動レイヤー）+ Tailwind CSS、テーマは2-2節のトークンで完全上書き** | フォーカストラップ・キーボード操作・ARIA属性等のアクセシブルな挙動を実装コストなしで得られるため、コンポーネント（Dialog, Table, DropdownMenu等）はshadcn/uiを採用する。ただし既定の見た目（配色・角丸・フォント）はそのまま使わず、Claude Designで確立したデザイントークンで上書きする |
| LP（ランディングページ） | **カスタム（Tailwind CSSでトンマナ設計）** | ユーザー獲得の入口。「AIが生成したHTML/Markdownをそのまま保存・公開できる」という価値をビジュアルで訴求する必要があり、shadcnの定型感では差別化しにくい |
| `/p/[uuid]` `/c/[uuid]`のiframe外枠（ヘッダー／フッター） | LPと同じトンマナのカスタム | 公開URLは第三者に共有される二次的なLPとして機能するため |

iframe内部（ユーザー生成コンテンツそのもの）はデザインシステムの対象外。

**shadcn/uiの位置づけ（v7で明確化）**：shadcn/uiはRadix UIベースのアクセシブルな「振る舞い」の雛形であり、固定のビジュアルスタイルを強制するものではない。見た目はすべてCSS変数経由で上書き可能な設計になっているため、「shadcn/uiを使う」ことと「Claude Designで確立した独自のトーンにする」ことは矛盾しない。`tailwind.config`・`globals.css`のCSS変数を2-2節のトークンで上書きすることで両立させる。

なお、更新履歴のタイムラインやLPの「プロダクトを覗いてみる」セクションのブラウザ風フレームなど、shadcnのプリミティブに対応物がない独自ビジュアルについては、無理にshadcnコンポーネントへ当てはめず、自由なTailwind実装で構わない。

### 2-1. デザインの雰囲気

claude.aiに近い、温かみのあるニュートラル基調。テラコッタ〜朱色系のアクセントカラーを1色のみ使用し、余白を広めに取る。ダークモードはMVPでは対応しない（ライトモードのみ）。

### 2-2. デザイントークン（v7で追加）

Claude Designでの検証を経て確立した具体値。実装時はこれらをTailwindのテーマ設定・CSS変数として反映する。

| トークン | 値 | 用途 |
| --- | --- | --- |
| フォント（見出し・本文） | `Plus Jakarta Sans`（サンセリフ） | 見出し・本文とも統一。明朝体・セリフ体は使用しない。見出しは本文より太めのウェイトでコントラストをつける |
| フォント（コード的表示） | `JetBrains Mono` | バイトサイズ表示・URLなど、等幅が望ましい箇所 |
| アクセントカラー（メイン） | `#C1503A` | 主要なボタン・アクティブ状態・Publicバッジ等 |
| アクセントカラー（濃色バリアント） | `#B5523A` / `#7A3226` | ホバー状態、強調テキスト等 |
| アクセントカラー（淡色バリアント） | `#E4C4B4` | アクセントの背景敷き・淡いハイライト |
| 背景色（クリーム系、明→暗） | `#FBF6F0` → `#F3E9DD` → `#F3E4DA` → `#F1E7DB` → `#E8DCD0` | ページ背景、カード背景、階層による濃淡の使い分け |
| テキストカラー | `#2B2320`（濃）/ `#3D332C` / `#6B5D52` / `#8A7A6D`（淡） | 見出し・本文・補助テキストの階調 |
| 角丸 | 12px（小要素：バッジ・チップ等）〜32px（大きめのカード・ダイアログ）、丸ボタン・アバター等は`999px` | 要素のサイズ・重要度に応じて使い分け |
| 影 | `0 10px 28px -18px rgba(43,35,32,0.18)`（通常のカード等、控えめ）／`0 6px 16px -8px rgba(193,80,58,0.5)`（アクセントカラーを使う要素の強調影） | 強いドロップシャドウは使わない方針を維持 |



---

## 3. URL設計

| パス | 内容 | 認証 |
| --- | --- | --- |
| `/p/[uuid]` | 個別クリップページ | クリップの公開設定に準ずる |
| `/c/[uuid]` | コレクション一覧ページ | コレクションの公開設定に準ずる（詳細は4-5節） |
| `/admin` | 管理画面トップ（クリップ一覧） | 必須（本人のコンテンツのみ） |
| `/admin/pages/[uuid]` | クリップ詳細・プレビュー・更新履歴 | 必須 |
| `/admin/collections` | コレクション一覧 | 必須 |
| `/admin/collections/[uuid]` | コレクション詳細・所属クリップ管理・並び替え | 必須 |
| `/login` `/signup` | 認証 | - |

UUIDは自動生成。人間が読めるスラッグは不要。新規作成・編集用の専用ページは設けない（すべてダイアログで完結、詳細は6章・7章）。

---

## 4. セキュリティ設計

### 4-1. XSS対策：別ドメインiframeによるコンテンツ配信

```
[本オリジン] clipnote.paritto.dev/p/[uuid]
  └─ <iframe src="https://content.clipnote.paritto.dev/[uuid]?t=xxx"
             sandbox="allow-scripts allow-popups allow-modals allow-popups-to-escape-sandbox">
```

> **重要**：この隔離モデルにより、クリップ本文（HTML）に対してスクリプト等の無害化（サニタイズ）処理は行わない。本文中に`<script>`タグや任意のJSが含まれていても、実行コンテキストが親オリジンから完全に分離されているため許容する設計とする。

### 4-2. sandbox属性の内訳

| フラグ | 採用 | 理由 |
| --- | --- | --- |
| `allow-scripts` | ○ | Mermaid等のスクリプト実行に必須 |
| `allow-popups` | ○ | 文書内の外部リンクを新規タブで開く |
| `allow-modals` | ○ | ライブラリ初期化エラー表示等、害は小さい |
| `allow-popups-to-escape-sandbox` | ○ | ポップアップで開いた先に制限を継承させない |
| `allow-same-origin` | ✕（付けない） | Cookie/Storage窃取を防ぐセキュリティの核 |
| `allow-forms` | ✕ | 文書中心の用途では不要 |
| `allow-top-navigation`系 | ✕ | フィッシング対策として付けない |

### 4-3. Hostヘッダー検証

コンテンツ配信エンドポイントで`Host`ヘッダーを検証し、コンテンツ配信ドメイン以外からのアクセスを拒否する。

### 4-4. 認証：署名付き短命トークン（HMAC-SHA256）

別ドメインにはセッションCookieが届かないため、署名付き短命トークン（**有効期限：2分**）で認証する。`content.clipnote.paritto.dev`側は、セッションCookieによるユーザー認証フローを一切持たない（＝ユーザーテーブルへの問い合わせやセッション検証は不要）ステートレスな設計とする。

```
① 本オリジン /p/[uuid] または /c/[uuid] にアクセス
      ↓ セッションCookieで認証確認 + クリップ／コレクションのアクセス権確認
② サーバーサイドでHMAC-SHA256署名付き短命トークンを生成
      ↓ CONTENT_TOKEN_SECRET（両ドメイン共通の環境変数）で署名
③ iframe src="https://content.clipnote.paritto.dev/[uuid]?t=xxx"
      ↓
④ content側でリクエストを処理する
```

トークンの構造：

```
payload = "{userId}:{uuid}:{exp}"
sig     = HMAC-SHA256(payload, CONTENT_TOKEN_SECRET)
token   = base64url("{payload}:{sig}")
```

#### `content.clipnote.paritto.dev`側の処理フロー（v8で明確化）

「トークン検証にDBアクセスが不要」という点と「コンテンツ本体の取得にはD1への読み取りアクセスが必要」という点を区別して記載する（旧版では両者が曖昧に「DBアクセス不要」と書かれていたため訂正）。

```
① Hostヘッダー検証（コンテンツ配信ドメイン以外からのアクセスを拒否）
② HMACトークンの検証（署名の正当性・有効期限・UUID一致確認）
   ← ここはHMAC計算のみで完結し、DBアクセスは不要
③ D1からUUIDで該当するpages行を1件取得
   ← ここはD1への読み取りアクセスが必要（トークン検証とは独立した処理）
④ content_typeに応じて出力：
   - html：pages.contentをそのままレスポンス（Content-Type: text/html）
   - markdown：unified/remark/rehypeでHTMLに変換してからレスポンス
⑤ 本文の無害化（サニタイズ）は行わない（4-1節の方針の通り）
```

UUID一致確認により他クリップのトークン流用を防止。

#### トークンの自動更新

有効期限が2分と短いため、閲覧者がタブを開いたまま長時間滞在した場合にiframeの再読み込み等でトークン切れエラーが発生しうる。これを避けるため：

- `/p/[uuid]`・`/c/[uuid]`の親ページ側で、**90秒間隔**で新しいトークンをサーバーから取得し、iframeの`src`属性を新トークンで書き換える（初回表示から2分経過する前に更新する設計）
- 更新はiframeの再読み込みを伴わない形（`src`のクエリパラメータのみ差し替え）を基本とするが、ブラウザ実装によっては同一パス+クエリ変更でも再読み込みが発生しうるため、実装時に挙動を確認する
- トークン取得APIが失敗した場合（セッション切れ等）は、静かに更新を諦め、次回の定期実行を待つ（エラー表示でユーザー体験を損なわない）

### 4-5. 公開設定の扱い（クリップ／コレクション）

| ケース | 挙動 |
| --- | --- |
| privateクリップ（認証済み本人がアクセス） | 通常通りトークン発行 |
| publicクリップ | 認証なしでもトークンを発行 |
| privateコレクション | 認証済み本人のみ`/c/[uuid]`にアクセス可。全所属クリップを一覧表示 |
| publicコレクション（本人以外／未認証がアクセス） | 認証不要で`/c/[uuid]`にアクセス可。**所属クリップのうち`private`のものは一覧から除外** |
| publicコレクション（本人が認証済みでアクセス） | 全所属クリップを一覧表示 |

### 4-6. OGP・シェア時のメタデータ（v5で追加）

要件定義書1章の「公開URLの共有によるユーザー獲得」という目的を踏まえ、公開URLがSNSやチャットでシェアされた際の見え方を定義する。

| 対象 | 挙動 |
| --- | --- |
| publicなクリップ／コレクション | タイトル・説明文を`og:title` / `og:description`として出力。`og:image`は当面固定のブランド画像（Clipnoteのロゴ等）を使用 |
| privateなクリップ／コレクション | OGPタグを一般的な固定文言のみにし、タイトルや説明などクリップ固有の情報は出力しない（存在の秘匿性を保つため、4-5節の非公開フィルタリングの思想と一貫させる） |

将来、10章のスクリーンショット生成機能が実装され次第、`og:image`を動的なプレビュー画像に差し替える段階的対応とする。

### 4-7. セキュリティまとめ

| 懸念 | 対策 |
| --- | --- |
| XSS（悪意あるHTMLのスクリプト実行） | iframe別ドメインサーブで親のCookie/Storageを隔離（本文自体の無害化は行わない） |
| iframe srcの書き換え | Hostヘッダー検証でブロック |
| 別ドメインでの認証 | HMAC-SHA256署名付き短命トークン（DBアクセスなしで検証） |
| 親ドメイン共有によるCookie漏出リスク | セッションCookieの`Domain`属性をホスト単体にスコープ |
| UUIDが漏れても非公開クリップを閲覧できる | トークンなしアクセスを拒否 |
| トークンの流用 | トークン内のUUIDと対象クリップのUUIDを一致確認 |
| 公開コレクション経由で非公開クリップの存在が露見する | 一覧そのものから除外（存在の痕跡を残さない） |
| 公開コレクション経由で非公開クリップの存在がOGP等から露見する | privateなコンテンツの固有情報をOGPに出力しない |
| 長時間閲覧時のトークン失効 | iframeの定期的な自動更新 |

---

## 5. コンテンツバリデーション

セキュリティ目的ではなく、UX・データ整合性・インフラコスト管理を目的とする。

### 5-1. 形式（HTML/Markdown）の妥当性

| チェック内容 | 扱い |
| --- | --- |
| 完全なHTML文書構造（`<html>`/`<body>`必須） | 要求しない（AI生成の断片的なマークアップを許容する） |
| タグの閉じ忘れなどの構文エラー | チェックしない（ブラウザの寛容なパースに委ねる） |
| Markdownとしての妥当性 | チェックしない |
| 空文字列でないか | チェックする |
| 有効なUTF-8か | チェックする |
| 選択した形式とペースト内容の食い違い | 警告のみ（ブロックしない） |

### 5-2. データサイズの上限

| 項目 | 値 | 備考 |
| --- | --- | --- |
| 1クリップ（1バージョン）あたりの本文サイズ上限 | **1MB（1,048,576 bytes）** | Cloudflare D1の行サイズ上限2MBに対し安全マージンを確保 |
| 判定基準 | バイト数（UTF-8エンコード後） | 文字数ではなくバイト数で判定 |
| バージョン10件保持時の1クリップあたり最大ストレージ | 約10MB | 11章の`page_versions`参照 |

- クライアント側でリアルタイムにバイト数を表示し、上限接近時は警告色にする
- サーバー側でも必ず再チェックする

### 5-3. ファイル読み込み時の追加チェック

- 拡張子が`.html` `.htm` `.md` `.markdown` `.txt`以外の場合は警告
- `File.size`を用いて、実際にテキストとして読み込む前にサイズ上限を事前チェックする
- 文字化け検知：U+FFFD等の異常値が一定割合以上含まれる場合、UTF-8確認の警告を表示（保存はブロックしない）

---

## 6. 画面設計：クリップ管理

### 6-1. クリップ一覧（`/admin`）

**デスクトップ（`md`以上）：テーブル形式**

| 表示列 | 内容 |
| --- | --- |
| 形式バッジ | `HTML` / `MD` |
| タイトル | クリックでプレビューを新規タブで開く |
| 公開設定 | バッジ。クリックでその場でトグル（即時反映） |
| 所属コレクション | チップ表示（複数可） |
| 更新日時 | デフォルトソート軸 |
| 公開URLコピー | Publicなクリップの行に常時表示 |
| 操作 | プレビュー／メタデータ編集／コンテンツ更新／更新履歴／削除（オーバーフローメニュー） |

**モバイル（`md`未満）：カード形式**

```
┌───────────────────────────────┐
│ [HTML] ● Public                 │
│ 今日のAI議事録まとめ               │
│ コレクション: 議事録 / 仕事メモ      │
│ 更新: 2026-08-04                 │
│                                 │
│ [ 🔗コピー ] [ プレビュー ] [ ⋯ ]  │
└───────────────────────────────┘
```

将来検討：一覧アイテムへのサムネイル／プレビュー画像表示（10章参照）。

### 6-2. 新規クリップ登録：ダイアログ

```
┌─────────────────────────────────┐
│ 新規クリップ                       │
├─────────────────────────────────┤
│ ┌───────────────────────────┐   │
│ │ ここに貼り付け、またはファイルを  │   │  ← ドラッグ&ドロップ対応
│ │ ドラッグ&ドロップ              │   │
│ └───────────────────────────┘   │
│                    [ファイルを選択] │
│                                   │
│ 形式: ○ HTML  ● Markdown          │
│ タイトル: [______________]        │
│ 公開設定: ○ Private ● Public      │
│ コレクション: [ 複数選択 ▾ ]（任意）│
│ 現在のサイズ: 12,400 / 1,048,576 bytes │
│                                   │
│              [ キャンセル ] [ 登録 ]│
└─────────────────────────────────┘
```

ファイルはサーバーへアップロードしない。`File.text()`でクライアント側のみでファイル内容を読み取り、本文へ反映し、`file.name`（拡張子除去）をタイトル初期値に反映する。サーバーへ送信するのは、貼り付け・ファイルいずれの経路でも本文の文字列＋メタデータのみ。

### 6-3. メタデータ編集：軽量ダイアログ

```
┌─────────────────────────────┐
│ クリップ情報を編集              │
├─────────────────────────────┤
│ タイトル: [______________]     │
│ 公開設定: ○ Private ● Public   │
│ コレクション: [ 複数選択 ▾ ]     │
│                               │
│           [ キャンセル ] [ 保存 ]│
└─────────────────────────────┘
```

### 6-4. コンテンツ更新：破壊的操作として分離

6-2節と同じ入力UIを再利用しつつ、次の点で新規登録と区別する。

- ダイアログタイトルを「クリップ本文の差し替え」のように明示
- 警告文：「既存の本文を上書きします。この操作は更新履歴として記録され、後から復元・ダウンロードできます」
- 現在の本文と新しい本文のサイズ差分を表示
- タイトル・公開設定・所属コレクションは変更されない旨を明記
- 実行ボタンは警告色（`destructive`バリアント）

内部処理：更新前の本文を`page_versions`へ退避してから`pages.content`を更新する（11-2節参照）。

### 6-5. 更新履歴・バージョンの復元／ダウンロード（v5で拡張）

`/admin/pages/[uuid]`から辿れる。差分（diff）表示は行わず、全文プレビューでの確認ベースとする。

**現在のバージョンにも過去のバージョンにも「ダウンロード」を提供する。復元は過去のバージョンにのみ提供する。**

```
┌─────────────────────────────────┐
│ 更新履歴                          │
├─────────────────────────────────┤
│ ● 現在のバージョン (v5)             │
│   2026-08-04 10:20    [ダウンロード] │
│                                   │
│ ○ v4  2026-08-03 18:02  [プレビュー][ダウンロード][復元] │
│ ○ v3  2026-08-01 09:15  [プレビュー][ダウンロード][復元] │
│ ○ v2  2026-07-30 14:40  [プレビュー][ダウンロード][復元] │
│ ○ v1  2026-07-28 11:00  [プレビュー][ダウンロード][復元] │
│                                   │
│ （直近10件のみ保持）                 │
└─────────────────────────────────┘
```

- 「プレビュー」：そのバージョンの内容を別ドメインiframeで簡易レンダリング表示
- 「ダウンロード」：クライアント側でBlob生成しファイル保存。サーバー通信不要（表示のために取得済みのテキストをそのまま利用）。ファイル名は`{タイトル}_v{番号}.{html|md}`形式（現在のバージョンは`{タイトル}.{html|md}`）
- 「復元」：実行すると、復元前の現在の内容も新しいバージョンとして履歴に退避してから上書きする（＝復元自体も1回の更新として記録される）
- ダウンロードは無害な操作のため確認ダイアログ不要（9章参照）
- ダウンロード機能は、クリップ本文のインライン編集機能（10章で将来検討とした機能）を持たない設計を補完する役割も持つ。「ダウンロード→手元で編集→コンテンツ更新で再登録」という運用を想定する

---

## 7. 画面設計：コレクション管理（v5で追加）

### 7-1. コレクション一覧（`/admin/collections`）

クリップ一覧（6-1節）と対称的な構成にする。

**デスクトップ（`md`以上）：テーブル形式**

| 表示列 | 内容 |
| --- | --- |
| コレクション名 | クリックで詳細画面へ |
| 説明 | 1行省略表示 |
| 公開設定 | バッジ、クリックでトグル |
| 所属クリップ数 | バッジ表示（例：「3件」） |
| 更新日時 | ソート軸 |
| 公開URLコピー | Publicなコレクションに常時表示 |

**モバイル（`md`未満）：カード形式**（クリップ一覧のカードと同様の構成）

新規作成：「＋ 新規コレクション」から名前・説明・公開設定のみの軽量ダイアログ。作成時点でのクリップ追加は任意とし、詳細画面から後付けできる。

### 7-2. コレクション詳細（`/admin/collections/[uuid]`）

```
┌─────────────────────────────────────┐
│ コレクション: 議事録まとめ                │
│ 説明: 週次AI議事録をまとめたもの            │
│ 公開設定: ● Public   [情報を編集]          │
├─────────────────────────────────────┤
│                          [+ クリップを追加] │
│ ⠿  [HTML] 今日のAI議事録まとめ      [外す] │
│ ⠿  [MD]   先週の振り返りメモ        [外す] │
│ ⠿  [HTML] プロジェクトA議事録       [外す] │
└─────────────────────────────────────┘
```

クリップが0件の場合は空状態表示（「まだクリップがありません。［＋ クリップを追加］」）とする。

### 7-3. 並び替えUX

デスクトップとモバイルで実装方式を分ける。

| 環境 | 方式 | 理由 |
| --- | --- | --- |
| デスクトップ（`md`以上） | ドラッグ&ドロップ（ドラッグハンドル`⠿`付き） | マウス操作なら誤操作が起きにくく直感的 |
| モバイル（`md`未満） | **並び替えモード＋上下移動ボタン** | タッチドラッグはスクロールと衝突しやすい。明示的な「並び替えモード」に入り`↑` `↓`ボタンで1件ずつ動かす |

```
モバイル（並び替えモード）
┌───────────────────────────────┐
│ 並び替え中            [完了]     │
├───────────────────────────────┤
│ [HTML] 今日のAI議事録まとめ  ↑ ↓  │
│ [MD]   先週の振り返りメモ    ↑ ↓  │
└───────────────────────────────┘
```

- 実装ライブラリは`dnd-kit`を推奨（キーボード操作にも対応しアクセシビリティ面で有利）
- 通信は「ドラッグ操作が完了した時点」で影響を受けた行の`sort_order`をまとめて1回のAPIコールとして送信する（書き込み回数を抑制）
- 並び替えモード中は、プレビュー等の通常操作を無効化する

### 7-4. クリップの追加

```
┌─────────────────────────────────┐
│ クリップを追加                     │
├─────────────────────────────────┤
│ 検索: [______________]             │
│                                   │
│ ☑ 今日のAI議事録まとめ               │
│ ☐ 先週の振り返りメモ                 │
│ ☐ プロジェクトA議事録（追加済み）      │
│                                   │
│              [キャンセル] [追加]     │
└─────────────────────────────────┘
```

自分の全クリップを検索・複数選択できるダイアログ。既に所属しているクリップはチェック済みで表示し、チェックの付け外しで追加／除外の両方を1つのUIで扱う。

### 7-5. 「外す」と「削除」の区別

| 操作 | 意味 | 実装箇所 | 確認ダイアログ |
| --- | --- | --- | --- |
| 「外す」 | `collection_pages`の関連付けのみ削除。クリップ自体は残る | コレクション詳細画面 | 不要（即時実行） |
| 「削除」 | クリップ本体（`pages`行）を削除。全コレクションから自動的に外れる | クリップ一覧・クリップ詳細画面 | 必要 |

### 7-6. コレクション情報の編集

```
┌─────────────────────────────┐
│ コレクション情報を編集           │
├─────────────────────────────┤
│ 名前: [______________]        │
│ 説明: [______________]        │
│ 公開設定: ○ Private ● Public  │
│                               │
│           [キャンセル] [保存]  │
└─────────────────────────────┘
```

---

## 8. 削除時のカスケード挙動（v5で追加）

| 削除対象 | 影響範囲 |
| --- | --- |
| クリップを削除 | `page_versions`（履歴）も連動削除。所属していた全コレクションの`collection_pages`からも自動的に外れる（コレクション自体は残る） |
| コレクションを削除 | `collection_pages`の関連付けのみ削除。**所属していたクリップ自体は削除されない** |

DB制約としては、`collection_pages.page_id`・`collection_pages.collection_id`・`page_versions.page_id`に`ON DELETE CASCADE`を設定し、アプリケーション側で個別に削除処理を書かずに整合性を保つ。

コレクション削除時の確認ダイアログには「所属していたクリップ自体は削除されません」という文言を明記し、誤解を防ぐ（9章の確認ダイアログ基準を参照）。

---

## 9. 確認ダイアログの一貫した基準（v5で追加）

破壊性・復元可能性に応じて、確認ダイアログの要否を統一する。

| 操作 | 確認ダイアログ | 理由 |
| --- | --- | --- |
| 公開設定のトグル | 不要（即時反映） | 何度でも戻せる |
| メタデータ編集の保存 | 不要 | 低リスク |
| コレクションから「外す」 | 不要 | クリップ自体は残る |
| バージョンのダウンロード | 不要 | 状態変更を伴わない無害な操作 |
| コンテンツ更新（本文差し替え） | **必要**（警告文＋サイズ差分表示） | 復元可能だが心理的インパクトが大きい |
| バージョンの復元 | **必要**（「vNの内容で上書きします」の確認） | 現在の内容を意図せず失うと感じやすい |
| クリップ削除 | **必要** | 完全に元に戻せない（バージョン履歴ごと消える） |
| コレクション削除 | **必要**（「クリップ自体は消えません」を明記） | 誤解されやすいため文言が重要 |

---

## 10. 将来検討（本書スコープ外）

- クリップ内容のインライン編集機能（エディタ＋ライブプレビュー）。MVPでは貼り付け／ファイル読み込みによる全文差し替えと、バージョンのダウンロードによって代替する。将来追加する場合も`page_versions`テーブルはそのまま活用できる
- 一覧アイテムのサムネイル／プレビュー画像表示（Cloudflare Browser Rendering等によるスクリーンショット生成が必要）。実装後は4-6節のOGP画像にも動的に反映する
- タグ機能
- MCPアップロード機能（`upload_page` / `update_page`、APIキー管理）
- ページ単位のパスワード保護・リンク共有

---

## 11. データモデル（詳細）

### `users`テーブル（better-auth管理）

better-authが管理する標準スキーマを利用（email/password認証）。

### `pages`テーブル

| カラム | 型 | 内容 |
| --- | --- | --- |
| id | uuid | PK |
| user_id | uuid | FK → users |
| title | text | クリップタイトル |
| content | text | コンテンツ本体（現在のバージョン） |
| content_type | enum | `html` / `markdown` |
| visibility | enum | `private` / `public` |
| created_at | timestamp | |
| updated_at | timestamp | |

> `content`のサイズ上限：1MB（1,048,576 bytes、UTF-8）。アプリケーション側でバリデーション（5-2節）。

### `collections`テーブル

| カラム | 型 | 内容 |
| --- | --- | --- |
| id | uuid | PK |
| user_id | uuid | FK → users |
| name | text | コレクション名 |
| description | text | コレクションの説明（任意） |
| visibility | enum | `private` / `public` |
| created_at | timestamp | |
| updated_at | timestamp | |

### `collection_pages`テーブル（中間テーブル）

| カラム | 型 | 内容 |
| --- | --- | --- |
| id | uuid | PK |
| collection_id | uuid | FK → collections（`ON DELETE CASCADE`） |
| page_id | uuid | FK → pages（`ON DELETE CASCADE`） |
| sort_order | int | コレクション内の表示順（DnD／上下ボタンで操作） |
| created_at | timestamp | |

### `page_versions`テーブル

コンテンツ更新時に、更新前の内容を退避するためのバージョン履歴テーブル。ダウンロード機能・復元機能の両方で参照される。

| カラム | 型 | 内容 |
| --- | --- | --- |
| id | uuid | PK |
| page_id | uuid | FK → pages（`ON DELETE CASCADE`） |
| content | text | その時点でのコンテンツ本体 |
| content_type | enum | `html` / `markdown` |
| version_number | int | 1から始まる連番（page_id内で一意） |
| created_at | timestamp | このバージョンが作られた日時 |

**運用方針**：

- `pages.content`は常に「現在のバージョン」を保持する
- 更新の流れ：①更新前の`pages.content`を`page_versions`へ退避（新しい`version_number`でINSERT）→ ②`pages.content`を新しい内容でUPDATE
- 復元の流れ：①復元前の現在の内容を`page_versions`へ退避 → ②選択したバージョンの内容で`pages.content`をUPDATE（＝復元も1回の更新として履歴に残る）
- ダウンロードの流れ：現在のバージョンは`pages.content`、過去のバージョンは該当行の`content`を、いずれもクライアント側でBlob化してファイル保存（サーバー側の追加処理は不要）
- 保持件数：直近10件を上限とし、超過分は古いものから削除する
- タイトル等の軽量なメタデータはバージョン管理の対象外（本文のみ）

### 将来追加予定のテーブル（本書のスコープ外）

| テーブル | 追加タイミング |
| --- | --- |
| `tags` / `page_tags` | タグ機能の実装時（MVP対象外） |
| `api_keys` | MCP対応フェーズ |

---

## 12. モノレポ構成（v6で追加）

`apps/`（デプロイされる実行単位）と`packages/`（共有ライブラリ）を分ける構成とする。

```
clipnote/
├── apps/
│   ├── web/           # メイン Next.js アプリ（clipnote.paritto.dev）
│   ├── content/        # コンテンツ配信 Worker（content.clipnote.paritto.dev）
│   └── mcp/            # MCPサーバー（mcp.clipnote.paritto.dev、次フェーズ）
├── packages/
│   ├── db/              # D1スキーマ・マイグレーション・クエリ層
│   └── content-token/    # HMAC署名・検証ロジック（web/contentで共有）
├── docs/
│   ├── requirements.md
│   └── design.md
├── package.json
└── pnpm-workspace.yaml
```

### 12-1. `apps/`の内訳

| フォルダ | 収める内容 | 理由 |
| --- | --- | --- |
| `apps/web` | 管理画面・LP・`/p/` `/c/`の外枠（Next.js全体） | ドメイン設計（1章）と1対1対応させる。デプロイ単位＝ディレクトリ単位が分かりやすい |
| `apps/content` | コンテンツ配信専用の軽量Worker（**Next.jsではなくHono等の軽量ルーターを使用、v8で明記**） | トークン検証はステートレス（4-4節）だが、コンテンツ本体の取得にはD1読み取りアクセスが必要。単純なリクエスト処理のみのため、Next.js/Reactランタイムのオーバーヘッドを避け、バンドルサイズ・コールドスタートを最小化する軽量な構成にする。Markdown→HTML変換（unified/remark/rehype）もこのWorker内で行う |
| `apps/mcp` | MCPサーバー（次フェーズ） | 実装は先でも、フォルダとしては最初から確保しておき、3ドメイン構成を一目で分かるようにする。中身は空でも良い |

### 12-2. `packages/`の内訳と切り出し方針

「2つ以上の`apps`から参照されるもの」だけを`packages/`に切り出す方針とし、MVPの規模で過剰分割にならないようにする。

| パッケージ | 収める内容 | 理由 |
| --- | --- | --- |
| `packages/db` | D1スキーマ定義・マイグレーション（11章のテーブル定義） | `web`（クリップ・コレクションのCRUD）と`content`（UUIDからのクリップ本体取得、v8で明記）の両方がD1への読み取り・書き込みを行うため共有パッケージ化する。将来`mcp`もクリップの読み書きで同じスキーマを参照する |
| `packages/content-token` | HMAC-SHA256のトークン生成・検証ロジック（4-4節の`payload`構造） | `web`（生成）と`content`（検証）の両方が同じロジックを持つ必要がある。2箇所に別々実装すると片方だけ仕様変更が漏れる事故に繋がり、セキュリティ設計（4章）の一貫性が崩れる原因になるため、必ず共有パッケージとして切り出す |

shadcn/uiのコンポーネント等、現時点で`web`のみが使うものは`apps/web`内に置き、無理に`packages/ui`のような形で切り出さない。`apps/mcp`の実装が始まり共有ニーズが出た時点で、`packages/`を段階的に増やす。

### 12-3. ツール構成

`pnpm workspaces` + `turborepo`の組み合わせを推奨する。`turbo.json`でapp単位のビルド・デプロイタスクを分離できるため、「`content`だけ再デプロイする」といった単位でのタスク実行に対応しやすい。

---

## 13. 関連ドキュメント

- Clipnote要件定義 v5：本書が詳細設計を担当する上位ドキュメント
- Clipnote要件定義 v1（2026-05-24）
